# ═══════════════════════════════════════════════════════════════════════════════
# CI/CD · Validación de Plugins Claude Code
# ═══════════════════════════════════════════════════════════════════════════════

name: Validar Plugins Claude Code

on:
  pull_request:
    paths:
      - "plugins/**"
      - ".claude/plugins/**"
      - "plantillas/plugins/**"
      - "plugins/validar_plugin.py"
      - ".github/workflows/validar-plugins.yml"
  push:
    branches: [main, master]
    paths:
      - "plugins/**"
      - ".claude/plugins/**"
      - "plantillas/plugins/**"
      - "plugins/validar_plugin.py"
  workflow_dispatch:
    inputs:
      plugin:
        description: "Directorio específico del plugin a validar (opcional)"
        required: false
        default: ""

jobs:
  descubrir:
    name: Descubrir plugins
    runs-on: ubuntu-latest
    outputs:
      plugins: ${{ steps.discover.outputs.plugins }}
    steps:
      - uses: actions/checkout@v4
      - id: discover
        run: |
          PLUGINS=()
          for base in "plugins" ".claude/plugins"; do
            if [ -d "$base" ]; then
              for dir in "$base"/*; do
                if [ -d "$dir" ] && [ -f "$dir/plugin.json" ]; then
                  PLUGINS+=("$dir")
                fi
              done
            fi
          done
          if [ -n "${{ github.event.inputs.plugin }}" ]; then
            PLUGINS=("${{ github.event.inputs.plugin }}")
          fi
          if [ ${#PLUGINS[@]} -eq 0 ]; then
            echo 'plugins=[]' >> $GITHUB_OUTPUT
            exit 0
          fi
          JSON=$(printf '%s\n' "${PLUGINS[@]}" | jq -R . | jq -s .)
          echo "plugins=$JSON" >> $GITHUB_OUTPUT

  validar:
    name: Validar ${{ matrix.plugin }}
    runs-on: ubuntu-latest
    needs: descubrir
    if: ${{ needs.descubrir.outputs.plugins != '[]' }}
    strategy:
      fail-fast: false
      matrix:
        plugin: ${{ fromJson(needs.descubrir.outputs.plugins) }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: |
          python -m pip install --upgrade pip
          pip install pyyaml
      - run: |
          echo "::group::Validando ${{ matrix.plugin }}"
          python plugins/validar_plugin.py "${{ matrix.plugin }}" --strict
          echo "::endgroup::"

  resumen:
    name: Resumen
    runs-on: ubuntu-latest
    needs: validar
    if: always()
    steps:
      - run: |
          if [ "${{ needs.validar.result }}" == "success" ]; then
            echo "✅ TODOS LOS PLUGINS PASARON"
            exit 0
          else
            echo "❌ ALGUNOS PLUGINS FALLARON"
            exit 1
          fi
