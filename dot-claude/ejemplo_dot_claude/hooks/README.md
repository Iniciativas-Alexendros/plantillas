# Hooks

Directorio para hooks de ciclo de vida de Claude Code.

Cada archivo `.yaml` define un interceptor (PreToolUse, PostToolUse, etc.).
Consulta `hooks/plantilla_hook/` en el sistema de plantillas.
