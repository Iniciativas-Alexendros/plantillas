# Commands

Directorio para comandos slash personalizados (`/mi-comando`).

Cada archivo `.md` define un comando con su prompt y metadata.
Consulta `commands/plantilla_command/` en el sistema de plantillas.
