# ═══════════════════════════════════════════════════════════════════════════════
# CI/CD · Validación del módulo knowledge
# ═══════════════════════════════════════════════════════════════════════════════

name: Validar Knowledge

on:
  pull_request:
    paths:
      - "knowledge/**"
      - "validadores/**"
      - ".github/workflows/validar-knowledge.yml"
  push:
    branches: [main, master]
    paths:
      - "knowledge/**"
      - "validadores/**"
  workflow_dispatch:

jobs:
  validar:
    name: Validar plantilla + ejemplo
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.12"

      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install pyyaml

      - name: Validar ejemplo (strict)
        run: |
          echo "::group::ejemplo_knowledge.md"
          python knowledge/validar_knowledge.py knowledge/ejemplo_knowledge.md --strict
          echo "::endgroup::"

      - name: Validar plantilla (no-strict, placeholders permitidos)
        run: |
          echo "::group::plantilla_knowledge.md"
          python knowledge/validar_knowledge.py knowledge/plantilla_knowledge.md || true
          echo "::endgroup::"
