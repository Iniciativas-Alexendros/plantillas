# ═══════════════════════════════════════════════════════════════════════════════
# CI/CD · Validación de Skills Claude Code
# ═══════════════════════════════════════════════════════════════════════════════

name: Validar Skills Claude Code

on:
  pull_request:
    paths:
      - "skills/**"
      - ".claude/skills/**"
      - "plantillas/skills/**"
      - "skills/validar_skill.py"
      - ".github/workflows/validar-skills.yml"
  push:
    branches: [main, master]
    paths:
      - "skills/**"
      - ".claude/skills/**"
      - "plantillas/skills/**"
      - "skills/validar_skill.py"
  workflow_dispatch:
    inputs:
      skill:
        description: "Directorio específico de la skill a validar (opcional)"
        required: false
        default: ""

jobs:
  descubrir:
    name: Descubrir skills
    runs-on: ubuntu-latest
    outputs:
      skills: ${{ steps.discover.outputs.skills }}
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - id: discover
        run: |
          SKILLS=()
          for base in "skills" ".claude/skills"; do
            if [ -d "$base" ]; then
              for dir in "$base"/*; do
                if [ -d "$dir" ] && [ -f "$dir/SKILL.md" ]; then
                  SKILLS+=("$dir")
                fi
              done
            fi
          done
          if [ -n "${INPUT_SKILL}" ]; then
            SKILLS=("${INPUT_SKILL}")
          fi
          if [ ${#SKILLS[@]} -eq 0 ]; then
            echo 'skills=[]' >> $GITHUB_OUTPUT
            exit 0
          fi
          JSON=$(printf '%s\n' "${SKILLS[@]}" | jq -R . | jq -s .)
          echo "skills=$JSON" >> $GITHUB_OUTPUT

  validar:
    name: Validar ${{ matrix.skill }}
    runs-on: ubuntu-latest
    needs: descubrir
    if: ${{ needs.descubrir.outputs.skills != '[]' }}
    strategy:
      fail-fast: false
      matrix:
        skill: ${{ fromJson(needs.descubrir.outputs.skills) }}
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.12"
      - run: |
          python -m pip install --upgrade pip
          pip install pyyaml
      - run: |
        env:
          ITEM: ${{ matrix.skill }}
          echo "::group::Validando ${ITEM}"
          python skills/validar_skill.py "${ITEM}" --strict
          echo "::endgroup::"

  resumen:
    name: Resumen
    runs-on: ubuntu-latest
    needs: validar
    if: always()
    steps:
      - run: |
          if [ "${{ needs.validar.result }}" == "success" ]; then
            echo "✅ TODAS LAS SKILLS PASARON"
            exit 0
          else
            echo "❌ ALGUNAS SKILLS FALLARON"
            exit 1
          fi
